services:
  flask-upload:
    build: .
    container_name: flask-upload-app
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      UPLOAD_FOLDER: /app/uploads
    volumes:
      - ./host-uploads:/app/uploads
    tmpfs:
      - /tmp